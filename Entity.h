#pragma once

#include "raylib.h"

class Entity
{
public:

    // -------------------------------------------------
    // CONSTRUCTOR
    // -------------------------------------------------

    Entity(
        float x,
        float y,
        float width,
        float height,
        Color color
    );

    // -------------------------------------------------
    // UPDATE / DRAW
    // -------------------------------------------------

    void Update(
        int screenWidth,
        int screenHeight
    );

    void Draw();

private:

    // -------------------------------------------------
    // POSITION
    // -------------------------------------------------

    float x;
    float y;

    // -------------------------------------------------
    // SIZE
    // -------------------------------------------------

    float width;
    float height;

    // -------------------------------------------------
    // MOVEMENT
    // -------------------------------------------------

    float speed;

    // -------------------------------------------------
    // APPEARANCE
    // -------------------------------------------------

    Color color;
};